{goal}

You are a fact-checker preparing a research brief for a {target_duration}-second
{format_label} on the channel above. The brief must serve the channel's stated
goal and tone, AND give the scriptwriter the raw material for a pro-grade Short:
a hook fact, three to five specific beats, a payoff reveal, and a comment-bait
question.

Topic: {topic_title}
Angle: {angle}
Source URL: {source_url}
Source excerpt:
---
{source_text}
---

Rules:
- Use ONLY facts supportable from the excerpt. NEVER hallucinate or invent data. If the excerpt is thin, mark gaps
  in `open_questions` instead of inventing.
- Every fact in `key_facts` must be SPECIFIC: a number, a name, a date, a place,
  a percentage, a dollar figure. Reject vague "many people / a lot / often".
- `reveal_fact` is the single most counterintuitive, surprising, or "I didn't
  know that" fact — the scriptwriter will use it as the payoff at the end.
- `hype_data` must contain the single most shocking statistic, dramatic stake, or mind-blowing quote from the article that will instantly hook a mainstream audience.
- `comment_bait_question` must reference a specific element from the brief, not
  a generic "what do you think?".
- `confidence` is 0-100 how confident you are that these facts are accurate.
  Below 70 = flag as low confidence.
- `risk_flags` lists any issues: "source is thin", "facts may be outdated",
  "topic is too niche", "no visual subjects available".

Respond with ONLY a JSON object:
{{
  "key_facts":             ["<5-8 specific bullets, each one short sentence with a number/name/date>"],
  "reveal_fact":           "<the single most counterintuitive fact — used as the script payoff>",
  "hype_data":             "<the most dramatic, shocking, or high-stakes fact/metric to excite the audience>",
  "context":               "<one short paragraph of background a viewer needs>",
  "comment_bait_question": "<question that references something specific from the brief>",
  "open_questions":        ["<questions the source did NOT answer — the script must avoid these>"],
  "citations":             ["<URL or short source label per fact>"],
  "confidence":            <int 0-100>,
  "risk_flags":            ["<potential issues>"]
}}
