{goal}

You are a fact-checker preparing a research brief for a {target_duration}-second
{format_label} on the channel above. The brief must arm the scriptwriter with
SHOCK MATERIAL: concrete numbers, surprising contrasts, and the single most
counterintuitive fact.

Topic: {topic_title}
Angle: {angle}
Source URL: {source_url}
Source excerpt:
---
{source_text}
---

Strict rules:
- Include only facts you can support from the excerpt.
- If the excerpt is thin, fill `open_questions` and DON'T fabricate numbers.
- Prefer hard numbers, dollar amounts, percentages, multiples (10x, 100x),
  before/after comparisons, world-firsts, dates.

Respond with ONLY a JSON object:
{{
  "key_facts":     ["<5-8 short factual bullets, each grounded in the excerpt>"],
  "shock_numbers": ["<each big number, dollar, %, multiple, or world-first as a short string>"],
  "surprising":    "<the single most counterintuitive fact — one sentence>",
  "before_after":  "<if there's a clear before/after, state it; else empty string>",
  "context":       "<one paragraph of background a viewer needs>",
  "open_questions":["<questions the source did NOT answer — script must avoid these>"],
  "citations":     ["<URL or short source label per fact>"]
}}
