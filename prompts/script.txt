{goal}

You are writing a {target_duration}-second {format_label} for the channel above,
in the niche: {niche_title}.
Voice persona: {voice_style}.
Visual style: {visual_style}.

Topic: {topic_title}
Angle: {angle}

Research brief:
{research_brief}

Hard rules:
- Total narration target: {target_duration} seconds (~{target_words} words at {wpm} wpm).
- Exactly {num_scenes} scenes.
- First {hook_max_seconds} seconds = a stop-the-scroll hook (no "hey guys", no slow intro).
- Every scene is rendered as a MOVING video clip (generated motion or stock footage). Each visual_prompt should describe a vivid scene that looks good with subtle camera/subject motion (real subjects, settings, action implied) — avoid abstract text overlays, charts, or static diagrams.
- End with a question or CTA that makes people comment, consistent with the channel goal.
- Title <= {title_max_chars} chars, no clickbait lies.
- Description includes 1 source URL and the disclosure: "{ai_disclosure}".
- Hashtags: exactly {hashtags_count} distinct, lowercase hashtags, dynamically chosen for THIS specific video. Always include #shorts as the first one. The remaining {hashtags_count_minus_one} must mix: niche tags, topic-specific keywords from the script, and audience-style tags. No duplicates, no spaces, each tag <= 25 characters.
- Thumbnail text: 3-4 punchy words max.

Respond with ONLY this JSON shape, no prose:
{{
  "hook": "<first {hook_max_seconds}-second hook line>",
  "scenes": [
    {{
      "narration":     "<one sentence, spoken>",
      "duration_sec":  <int 3-7>,
      "visual_prompt": "<detailed SDXL prompt: a real subject in a real setting, photoreal or matching style, that will look good when subtle motion is added>",
      "broll_keywords":["<3-5 keywords for stock-video fallback>"]
    }}
  ],
  "cta": "<final line that drives a comment or follow>",
  "title":            "<= {title_max_chars} chars",
  "description":      "<2-3 sentences + source URL + #hashtags + AI disclosure>",
  "hashtags":         ["#shorts", "#...", "...exactly {hashtags_count} entries total"],
  "thumbnail_prompt": "<SDXL prompt for the thumbnail background>",
  "thumbnail_text":   "<3-4 word punch>"
}}
